spring:
  profiles:
    active: dev

# Email configuration
  mail:
    host: localhost
    port: 1025
    username: hi
    password: hi
    properties:
      mail:
        smt:
          ssl:
            trust: "*"
          auth: true
          starttls:
            enable: true
          connectionTimeout: 5000
          timeout: 5000
          writetimeout: 5000
